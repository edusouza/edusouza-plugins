# Readme

Not `README.md`. The structural exemption covers exactly the four filenames the scaffolder writes,
so this is user-authored content: a page, audited like any other, with links that are real edges.
It points at [[concept_alpha]].
