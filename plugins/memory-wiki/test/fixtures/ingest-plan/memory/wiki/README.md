# This project's memory wiki

Stand-in for the page-schema asset `wiki-init.sh` copies into every new wiki. It is here so the
work order has a `README` to exclude alongside `index` and `log`; its body is never read.
